import { Box } from "@chakra-ui/react";
import Home from "../components/home/Home";

const HomePage = () => {
  return (
    <Box zIndex={"-2"}>
      <Home />
    </Box>
  );
};

export default HomePage;
