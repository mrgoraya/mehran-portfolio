import { Box } from "@chakra-ui/react";
import { ColorModeProps } from "../../types/colorMode";

// TODO: Add image here

const HomePageImage = ({ colorMode }: ColorModeProps) => {
  return <Box flex="1" flexBasis="0" position="relative"></Box>;
};

export default HomePageImage;
